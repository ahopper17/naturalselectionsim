version: '3.8'

services:
  api:
    build: .
    ports:
      - "5001:5001"
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
    volumes:
      - ./simulation:/app/simulation
      - ./app.py:/app/app.py
